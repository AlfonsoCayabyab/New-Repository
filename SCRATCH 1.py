from breezypythongui import CENTER, EasyFrame      class TaxCodeDemo(EasyFrame):       """Application window for the Currency converter."""       def __init__(self):         """Sets up the window and the widgets."""         EasyFrame.__init__(self, title = "Converter")            # Label and field          self.addLabel(text = "Enter the currency amount in cents and press the enter button",                       row = 0, column = 0)                   # Amount field to enter cents         self.addLabel(text = "Amount",                       row = 1, column = 0)         self.centsField = self.addFloatField(value = 0,                                              row = 1,                                              column = 1,width = 10,sticky = "NSEW")           # The Convert Button         self.addButton(text = "Convert", row = 2, column = 0,                        columnspan = 2, command = self.compute)                 #Quarters Field         self.addLabel(text = "Quarters",                       row = 3, column = 0)         self.quartersField = self.addFloatField(value = 0.0,                                            row = 3,                                            column = 1,                                            precision = 2)            #Dimes Field         self.addLabel(text = "Dimes",                       row = 4, column = 0)         self.dimesField = self.addFloatField(value = 0.0,                                            row = 4,                                            column = 1,                                            precision = 2)                                                                        #Nickels Field         self.addLabel(text = "Nickels",                       row = 5, column = 0)         self.nickelsField = self.addFloatField(value = 0.0,                                            row = 5,                                            column = 1,                                            precision = 2)                  #Pennies Field         self.addLabel(text = "Pennies",                       row = 6, column = 0)         self.penniesField = self.addFloatField(value = 0.0,                                            row = 6,                                            column = 1,                                            precision = 2)       # The event handler method for the button "convert"     def compute(self):         """Obtains the data from the input fields and uses         them to compute, which is sent to the         output field.                  US currency nickname conversion: a penny = 1 cent, a nickel = 5 cents, a dime = 10 cents and a quarter = 25 cents.                  """          amount=self.centsField.getNumber()         quarters=amount/25         dimes=amount/10         nickels=amount/5         pennies=amount/1          self.quartersField.setNumber(quarters)         self.dimesField.setNumber(dimes)         self.nickelsField.setNumber(nickels)         self.penniesField.setNumber(pennies)                    #Instantiate and pop up the window. TaxCodeDemo().mainloop()